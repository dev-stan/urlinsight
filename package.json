{
  "name": "urlinsight-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for FastAPI backend and SvelteKit frontend",
  "scripts": {
    "install:all": "npm install --prefix web && pip install -r api/requirements.txt",
    "dev:api": "uvicorn app.main:app --reload --app-dir api --port 8000",
    "dev:web": "npm --prefix web run dev",
    "dev": "npx concurrently --kill-others \"npm run dev:api\" \"npm run dev:web\"",
    "build:web": "npm --prefix web run build",
    "preview:web": "npm --prefix web run preview"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}